import React from 'react';

const CheckoutBtn = () => {
    return (
        <button className="btn btn-active btn-ghost text-white">Checkout Your food</button>

    );
};

export default CheckoutBtn;